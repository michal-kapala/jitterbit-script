<trans>
  // user-defined system variable null-initialization test
  niceTokenLol = $jitterbit.api.request.headers.Authentication;
  // default value-initialized system variable assigned to a global var
  $niceTokenLol = '$jitterbit.web_service_call.sync_response;
  lol nice';

  // tests for RegExMatch function
  // should return 4
  // should assign $dummy = "[abc]", $value = "[", $value2 = "abc", $value3 = "]"
  // value4 should not get assigned
  // this impl matches the whole string as 1st, compare with JB example in the docs
  result = RegExMatch("[abc]", "(\\[)(.*)(\\])", "dummy", "value", "value2", "value3", "value4");
  DebugBreak();
  
</trans>
